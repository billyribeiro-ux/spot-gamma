# Loaded by `vite build --mode desktop` (the Tauri bundle). The desktop app has
# no dev proxy, so it talks to the FastAPI backend directly. Override to point at
# a remote/managed backend if you aren't running the API on localhost.
VITE_API_BASE=http://localhost:8000
